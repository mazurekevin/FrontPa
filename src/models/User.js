
export default class User {
   constructor(username, password, name, id, role, token){
     this.username = username;
     this.password = password;
     this.name = name;
     this.id = id;
     this.role = role;
     this.token = token;

   }
 }